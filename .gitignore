# Notas internas/personales — nunca deben subirse a GitHub (repo BIOLAB es publico).
# Se conservan solo en la carpeta local de biolab-app.
CLAUDE.md
BIOLAB_SYSTEM.md
CHANGELOG.md

# Worktrees locales de desarrollo (subagent-driven-development)
.worktrees/

# Backups de datos exportados de la app — nunca al repo publico
biolab-backup-*.json
biolab-ci-backup-*.json
biolab_full_backup*.json

# Bitacora de inteligencia de laboratorio — datos propietarios, nunca al repo publico
docs/lab-intelligence/

# Evidencia fotografica de ensayos de laboratorio — datos reales, nunca al repo publico
# (2026-08-17: confirmado que nunca se subio a GitHub — ver git log/ls-tree completo,
# pero quedaba sin protegerse contra un git add -A futuro)
cilab/CILAB_EVIDENCIA/

# Mockups temporales del skill de brainstorming (superpowers)
.superpowers/

# Skills de Claude Code — describen metodologia/workflow interno del lab, nunca al repo publico
.claude/
